---
import BaseLayout from '../layouts/BaseLayout.astro'
import { ReviewsPage } from '../views/ReviewsPage'
import { SEO, SITE_URL } from '../data/site'
import { productReviewsJsonLd, productJsonLdDocument } from '../lib/seo'

const productJsonLd = productJsonLdDocument(productReviewsJsonLd())
const jsonLdExtra = [
  {
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Home', item: `${SITE_URL}/` },
      { '@type': 'ListItem', position: 2, name: 'Reviews', item: `${SITE_URL}/reviews` },
    ],
  },
]
---

<BaseLayout seo={SEO.reviews} jsonLdExtra={jsonLdExtra} productJsonLd={productJsonLd}>
  <ReviewsPage client:visible />
</BaseLayout>

